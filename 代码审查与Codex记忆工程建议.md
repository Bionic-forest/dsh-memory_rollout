# dsh-rollout 代码审查与 Codex 记忆工程建议

审查日期：2026-08-27  
审查对象：`dsh-rollout` 0.1.5，`main` 分支提交 `06f4887`  
审查性质：只读代码审查；本文只记录问题与维护建议，不代表问题已经修复。

## 一、结论

项目方向是成立的：它已经搭出了“会话草稿 → 可搜索注册表 → 短摘要注入 → 按需展开证据”的基本骨架，也意识到了上下文预算、幂等整合、来源追踪和主动遗忘等关键问题。

但当前实现仍属于可供作者自用的原型，不适合作为唯一记忆副本或直接向普通用户承诺可靠恢复。最优先的问题不是界面和代码风格，而是以下三项：

1. 导入失败可能导致长期记忆表被部分或全部清空，而自动备份无法恢复这张表；
2. 次级候选会话会反复把旧草稿完整嵌套进新草稿，造成内容套娃和文件膨胀；
3. 默认配置会在会话结束后把会话内容交给语言模型提炼，与“只在用户显式要求时写入”的产品承诺冲突。

建议先把数据安全和自动管线做稳，再继续扩大功能。

## 二、审查发现

### P0：导入失败时，自动备份不足以恢复长期记忆表

导入前的备份只复制 `memories/` 文件目录：

- `lib/index.js:1685-1689`

长期记忆条目实际位于 storage-domain 的 `entries` 表中，不属于上述目录。导入随后会先清空旧表，再逐条写入新表：

- `lib/index.js:1705-1721`

如果写入中途失败，可能出现以下状态：

1. 原表已经被删除；
2. 新表只恢复了一部分；
3. `memories-backup-*` 只包含文件，不包含原表；
4. 页面仍可能显示“导入前已备份”，但这份备份不能完整回滚。

维护建议：

- 导入前同时保存文件树和长期记忆表；
- 在临时目录和临时表中完成全部解码、schema 校验和写入；
- 全部成功后再进行最终切换；
- 任一步失败都保持原文件树和原表不变；
- 增加“故意在第 N 条写入时报错”的故障注入测试。

验收标准：任意一步抛错后，导入前的文件和长期记忆条目逐项不变，用户不需要手工拼接备份才能恢复。

### P0：所谓“替换式导入”没有删除旧文件

README 把导入描述为 replace semantics（替换语义），见：

- `README.md:117`
- `README.zh.md:120`

实际实现只把备份中的文件逐个覆盖进现有 `memories/`，没有移走或清空旧树：

- `lib/index.js:1692-1703`

因此，旧目录中存在、导入包中不存在的草稿或 note 会继续保留。随后 `integrate()` 还可能把这些旧文件重新编入 `MEMORY.md`，使被移除的记忆“复活”。

维护建议：先把导入包完整恢复到新的临时根目录，验证成功后以目录切换完成替换；不要在正在使用的根目录中原地叠加。

验收标准：导入完成后的文件集合必须与导出包中的文件集合完全一致，不多也不少；运行 `integrate()` 后不得重新出现导出包中不存在的旧草稿。

### P0：次级候选草稿会反复套娃

自动管线对次级候选的处理链路如下：

1. 从状态表选择已经闲置的旧会话：`lib/index.js:831-845`；
2. 读取该会话现有草稿的完整 Markdown：`lib/index.js:868-875`；
3. 把这份完整 Markdown 当成新的 `rollout_summary`：`lib/index.js:896-903`；
4. 再增加一层元数据头并覆盖原文件：`lib/index.js:905`。

候选判断没有检查 `summarizedAt >= lastActivityAt`。因此，同一闲置会话可以在之后的管线运行中再次入选，每次都把上一版完整草稿包进下一版。

维护建议：

- 如果会话自上次摘要后没有新活动，直接跳过；
- 次级候选必须重新从持久化会话事件读取原始消息，不能把已经格式化的草稿当作原始输入；
- 如果确实只需重建索引，直接复用现有草稿，不要重写草稿文件。

验收标准：对同一个没有新增消息的会话连续触发十次管线，草稿内容和文件大小应保持不变。

### P1：默认自动提炼与“显式写入”承诺冲突

项目描述和注入提示多次强调“只有用户显式要求才更新记忆”：

- `package.json:4`
- `lib/index.js:1023-1025`
- `lib/index.js:1032-1035`

但默认配置为 `autoTrigger: 'sessionEnd'`：

- `lib/index.js:62-63`

会话结束后会自动读取会话内容并调用配置的语言模型：

- `lib/index.js:1387-1395`
- `lib/index.js:701-739`

这带来三个产品层问题：

- 用户可能不知道关闭会话会消耗模型额度；
- 会话中的路径、代码、个人信息或密钥可能被发送给外部模型提供商；
- “显式写入”和“默认自动提炼”是两种不同的隐私模型，不能同时作为默认承诺。

维护建议：二选一并写清楚。

- 保守方案：默认 `autoTrigger: 'off'`，由用户主动开启；
- 自动方案：安装或首次运行时明确告知数据范围、模型去向和额度消耗，并取得用户选择。

验收标准：关闭自动触发时，结束会话不得写草稿、不得调用语言模型、不得消耗每日管线额度。

### P1：`maxExtractPerTrigger` 存在边界错误

触发会话会先无条件加入候选列表：

- `lib/index.js:822-829`

次级候选循环在加入候选之后才检查数量上限：

- `lib/index.js:833-845`

因此当 `maxExtractPerTrigger=1` 时，候选列表仍可能包含“触发会话 + 一个次级会话”，实际处理两个。

维护建议：在加入任何候选之前检查剩余名额，或者先确定触发会话是否计入该参数，并把语义写入配置说明。

验收标准：配置为 1、2、10 时，实际写入草稿的会话数均不得超过对应上限。

### P1：导入接口缺少资源上限和完整校验

请求体会被完整收集进内存：

- `lib/index.js:1442-1448`

导入时没有限制文件数量、单文件大小、解码后总大小，也没有在改动现有数据前验证整个包：

- `lib/index.js:1692-1703`

此外，`Buffer.from(value, 'base64')` 对部分非法输入会宽松解码，不能代替备份完整性校验。

维护建议：

- 限制 HTTP 请求体大小；
- 限制文件数量、单文件大小和总解码大小；
- 校验备份格式版本、重复路径、目录冲突和每条 entry 的 schema；
- 导出时记录清单摘要，导入时先验证再写入；
- 摘要用于发现损坏，不应替代事务式导入和可恢复备份。

### P2：管理页没有实现 README 所说的完整浏览功能

后端实现了 `/dsh-rollout/overview`，返回 summary、registry、drafts、notes 和 entries：

- `lib/index.js:1503-1526`

前端没有请求该接口，只读取 `/entries` 和 `/config`：

- `lib/client.js:56-78`

页面最终只渲染长期条目和设置：

- `lib/client.js:251-354`

因此“浏览摘要、注册表、逐会话草稿和笔记”的文档承诺当前并未落地。

维护建议：在功能完成前缩小 README 描述，或者把 overview 接到只读分栏中；不要让未使用的后端接口长期冒充已交付功能。

### P2：没有自动化测试和标准测试入口

仓库没有测试目录或测试文件，`package.json` 也没有 `scripts.test`。维护指南目前只要求：

```text
node --check lib/index.js
node --check lib/client.js
```

语法检查只能证明 JavaScript 可以解析，不能证明导入、限流、候选选择、原子恢复和事件触发正确。

维护建议：先使用 Node.js 内置测试运行器（Node test runner），避免为原型项目额外引入大型测试框架。优先覆盖本文 P0/P1 问题，再补 UI 测试。

## 三、建议的修复顺序

### 第一阶段：先保数据

1. 重做导入为“完整验证 → 临时恢复 → 原子切换”；
2. 把长期记忆表纳入可恢复备份；
3. 增加导入体积、数量、格式与 schema 限制；
4. 为失败回滚编写自动化测试。

### 第二阶段：修正自动管线

1. 阻止已摘要且无新活动的会话重复入选；
2. 禁止把格式化草稿重新当成原始会话文本；
3. 修复 `maxExtractPerTrigger` 边界；
4. 明确每日额度究竟统计“触发次数、语言模型调用次数还是成功整合次数”。

### 第三阶段：对齐产品承诺

1. 决定默认是显式写入还是自动写入；
2. 补充隐私、模型调用和费用说明；
3. 将 overview 接入前端，或从 README 删除未实现描述；
4. 增加真实的 `test` 脚本和发布前检查。

## 四、对 Codex 记忆工程的理解

### 4.1 能确认什么，不能确认什么

OpenAI 官方公开文档能够确认：Codex 会分层发现和合并 `AGENTS.md`，从全局范围一路走到当前工作目录；离当前目录更近的规则拥有更高优先级；同时存在默认 32 KiB 的项目指令预算。这说明 Codex 很重视三件事：**作用域、优先级和上下文预算**。

官方资料：<https://learn.chatgpt.com/docs/agent-configuration/agents-md>

截至本文审查日期，官方公开文档没有完整披露 Codex 跨会话记忆的内部提炼、整合和淘汰流水线。因此下面是针对本项目的工程分析，不应写成“OpenAI 官方内部实现说明”。

### 4.2 Codex 记忆工程最值得借鉴的不是目录名，而是分层责任

可以把跨会话记忆理解成医院病历系统：

| 层 | 类比 | 应保存什么 | 不应承担什么 |
| --- | --- | --- | --- |
| 当前会话记录 | 当次完整病历 | 原始事实、消息、工具结果 | 不直接全部塞进下一次会话 |
| 会话草稿 | 出院小结 | 本次稳定结论、决策、踩坑和结果 | 不冒充长期真理 |
| `MEMORY.md` 注册表 | 病历索引 | 在哪里能找到什么、适用范围、证据位置 | 不复制所有病历正文 |
| `memory_summary.md` | 首页重要提示 | 少量高价值、跨任务仍有用的规则和用户偏好 | 不成为无限增长的流水账 |
| ad hoc note | 病历更正申请 | 添加、删除、更新的明确请求 | 不绕过整合直接破坏正式记录 |

本项目已经模仿了这些层的名称，却还没有完全守住各层的责任。例如，次级候选把完整旧草稿重新包进草稿，等于把“出院小结”当成“原始病历”再次抄写；自动生成的 summary 主要列文件，而没有真正承担高价值总纲的筛选职责。

### 4.3 记忆系统首先是上下文预算系统，其次才是存储系统

把内容存到硬盘并不难，难点是下一次任务只带入正确的少量内容。

一个健康的读取链路应当是：

```text
当前问题
  → 判断历史是否相关
  → 从短总纲提取关键词
  → 搜索注册表
  → 只打开 1～2 份最相关证据
  → 判断是否过时
  → 回答并标明来源
```

这里追求的不是“尽量多想起来”，而是“少带错东西”。记忆误命中的危险往往高于漏命中：漏掉一条旧偏好，模型还可以询问；把过期决策当成当前事实，模型会自信地把任务做错。

因此建议把评价指标按以下顺序排列：

1. 记忆精确率：召回的内容有多少真的适用于当前任务；
2. 来源可追溯性：能否回到具体草稿和证据行；
3. 新鲜度：是否知道内容可能过时；
4. 可纠正性：用户能否准确删除或更新一条，而不是误伤同标签内容；
5. 召回率：最后才是“有没有尽量多记住”。

### 4.4 原始事实、模型摘要和正式记忆必须分开

语言模型生成的摘要不是事实数据库，它可能省略限定条件、混淆时间或把临时方案写成最终决策。

建议每条正式记忆至少保留：

- 稳定 ID；
- 记忆正文；
- 来源会话 ID；
- 来源文件及行号或事件范围；
- 创建时间和最后确认时间；
- 适用工作区或项目；
- 状态：有效、可能过时、已替代、已删除；
- 由谁产生：用户明确要求、人工界面、自动提炼。

自动提炼只能生成“候选记忆”。要成为高权重长期记忆，应满足更严格的规则，例如用户明确确认、同一事实被多次验证，或者维护者定义了可靠的升级条件。

### 4.5 “一会话一草稿”是好约束，但草稿必须单调可解释

“一会话一草稿”能防止会话之间相互覆盖，也便于追踪来源。不过它还需要明确更新语义：

- append：只追加新的阶段记录；
- replace：重新从原始会话生成完整草稿；
- no-op：没有新消息时保持字节不变。

当前项目混合了 replace 和“把旧输出当新输入”，导致套娃。建议保存 `last_event_id` 或 `last_summarized_at`，只有存在新事件时才更新；重建时始终以原始会话事件为输入。

### 4.6 幂等不能只看水印，还要看整个状态转换

项目已有 fingerprint（指纹）与 watermark（水印），这是正确方向。但真正的幂等不是“最终索引没有重复写”，而是：同一输入重复运行任意次数，所有可观察结果都相同。

应同时验证：

- 草稿内容不增长；
- 每日额度不被同一输入重复消耗；
- 长期条目不重复；
- summary 和 registry 不因文件遍历顺序变化而抖动；
- 失败重试不会造成半份导入或半份索引。

换句话说，水印只是幂等的一个工具，不是幂等本身。

### 4.7 压缩和跨会话记忆是两类问题

上下文压缩（compaction）解决的是“当前会话太长，如何继续”；跨会话记忆解决的是“另一个任务以后应该知道什么”。两者的保留标准不同：

- 压缩摘要可以保留当前任务的临时变量、未完成步骤和短期状态；
- 长期记忆应只保留跨会话仍然稳定、有复用价值的事实和决策。

如果同一个自动提炼结果同时服务两者，临时任务状态就容易污染长期记忆。建议将 precompact checkpoint（压缩前检查点）与 durable memory candidate（长期记忆候选）分成不同类型，再由整合阶段决定哪些内容可以升级。

### 4.8 “遗忘”不是删除按钮，而是记忆系统的核心能力

长期运行后，最常见的问题不是记不住，而是：

- 同一事实重复多条；
- 新决策已经替代旧决策；
- 临时路径和版本号过期；
- 共用标签删除时误伤其他记忆。

因此正式模型应支持“替代关系”，而不只是按 ID 或 tag 硬删除。例如新条目可以记录 `supersedes: <old-id>`，索引默认只展示当前有效版本，但仍保留历史证据和可回滚能力。

### 4.9 自动化程度越高，越需要可见性和同意边界

自动记忆通常会接触整段会话，因此设置页至少应展示：

- 自动提炼是否开启；
- 使用哪个 provider（提供商）和模型；
- 最近一次提炼的会话、时间、输入规模和结果；
- 今日调用次数和失败次数；
- 哪些内容只在本地，哪些内容发送给外部服务；
- 一键暂停和一键导出。

这不是单纯的界面美化，而是让用户理解系统正在替他保存和传递什么。

## 五、建议的目标架构

```text
不可变会话事件
      │
      ▼
候选提炼（可失败、可重试，不修改正式记忆）
      │
      ├── 当前会话检查点（服务 compaction）
      └── 长期记忆候选（带来源与置信状态）
                    │
                    ▼
整合器：去重、替代、作用域、新鲜度判断
                    │
          ┌─────────┴─────────┐
          ▼                   ▼
短总纲 memory_summary    注册表 MEMORY
          │                   │
          └──── 按需打开证据 ──┘
```

写入过程应遵守两条底线：

1. 原始会话事件不可被摘要过程覆盖；
2. 新状态完整验证并落盘成功前，旧正式状态始终可用。

## 六、最低发布门槛

在把项目从“作者自用原型”提升为“可邀请他人试用”前，建议至少完成：

- 修复本文所有 P0；
- 自动触发改为明确选择，并说明外部模型数据流；
- 导入失败可自动回滚文件与长期记忆表；
- 连续重复运行不会改变无新输入的草稿；
- 有一套可执行的 Node.js 自动测试；
- 管理页真实展示摘要、草稿、notes、最近管线活动和失败；
- README 明确说明哪些功能已验证、哪些仍未验证。

完成这些后，这个项目才算从“模仿 Codex 的目录结构”迈向“真正吸收 Codex 记忆工程的约束思想”。
